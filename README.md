# MSR Mirroring Policy Password Update Tool

## Usage
```bash
docker run --rm -it squizzi/msr-policy-updater \
    --username <username> \
    --password <password> \
    --msr-url <msr-url> \
    --poll-mirroring \
    --push-mirroring
```
